command:
  commandName
level:
  0
script: |
  def commandName(name, age)
    puts "Hello #{name} age#{age}"
    puts :pluginSample!
    subMethods
  end
  def subMethods
    puts "subMethods!!"
  end
